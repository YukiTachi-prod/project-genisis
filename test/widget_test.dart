// Home AI — placeholder widget test.
// Full integration tests require audio hardware; add unit tests here instead.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
